import Icons      from './_examples/Icons.jsx'
import IconsSrc   from './_examples/Icons.jsx?raw'
import { Example, Link } from '@abw/badger-website'

# Icon

## `library`

The `library` property can be used to define an alternate icon library for
the component.

See the <Link to="/library" text="Icon Library"/> pages for further
information about this.

<Example
  Component={Icons}
  code={IconsSrc}
/>
