import React    from 'react'
import Icon     from './_examples/Icon.jsx'
import IconSrc  from './_examples/Icon.jsx?raw'
import { Example } from '@abw/badger-website'

# Icon

The `Icon` component can be used to render an SVG icon.

<Example
  Component={Icon}
  code={IconSrc}
  caption="Icon"
  highlightLines="4"
  expand
/>
