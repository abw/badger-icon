import Icons from '@/examples/Icons.jsx'

# Modifiers

## Orientation

The `right`, `down` and `left` modifiers will rotate the icon by 90, 180 and
270 degrees respectively.  The `up` modifier doesn't do anything but is included for
completeness.

<Icons names="arrow arrow-up arrow-down arrow-left arrow-right"/>
