import Icons from '@/examples/Icons.jsx'

# Modifiers

## Shrink

The `shrink=N` suffix can be used to decrease the size of the icon content.
Each number corresponds to 1/16th of the icon size, e.g `shrink=8` will reduce
the size by 50%.

<Icons names="square square-shrink=2 square-shrink=4 square-shrink=6 square-shrink=8"/>
